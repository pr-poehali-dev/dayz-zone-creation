{
  "tests": [
    {
      "name": "GET /promocodes — список активных",
      "method": "GET",
      "path": "/",
      "expectedStatus": 200,
      "expectedBody": {"promocodes": []},
      "bodyMatcher": "partial"
    }
  ]
}
