{
  "tests": [
    {
      "name": "GET /config — Discord CLIENT_ID",
      "method": "GET",
      "path": "/config",
      "expectedStatus": 200,
      "expectedBody": {"clientId": ""},
      "bodyMatcher": "partial"
    }
  ]
}